This is a react/redux application using react bootstrap for the styling.
​
To run this application, you will need to add your OMDb api key to the /utils/keys.js.

Ex:
export const APIKey = '{Your OMDb api key here}';

​
